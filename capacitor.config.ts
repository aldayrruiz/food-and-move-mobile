import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.bluece.foodandmove',
  appName: 'Food And Move',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
